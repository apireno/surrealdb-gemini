/* Get Gemini token.

Insert your token into this function before importing into SurrealDB.

Returns:
    string:  token.
*/
DEFINE FUNCTION fn::get_gemini_token() {
    RETURN "<token - goes - here>"
};

